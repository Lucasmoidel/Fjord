//TODO: Make a key class with functions like isHeld(), isDown(), isUp(), etc...

class Input{
    public:
        //Input();
        void initKeyMap();
        bool isHeld();
        bool isDown();
        bool isUp();
};